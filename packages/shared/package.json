{
  "name": "@urlshortener/shared",
  "version": "1.0.0",
  "description": "Shared types, constants, and utilities for the URL shortener platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "node -e \"fs.rmSync('dist',{recursive:true,force:true})\"",
    "prebuild": "npm run clean",
    "watch": "tsc --watch"
  },
  "keywords": [
    "url-shortener",
    "shared",
    "types"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
